<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vmd="clr-namespace:FootprintViewer.Fluent.Designer"
             xmlns:panels="clr-namespace:FootprintViewer.Fluent.Views.BottomPanel"
             xmlns:controls="clr-namespace:FootprintViewer.Fluent.Controls"
             mc:Ignorable="d"
			 d:DesignWidth="300" d:DesignHeight="450"
             x:Class="FootprintViewer.Fluent.Views.BottomPanel.BottomPanelView"
			 d:DataContext="{x:Static vmd:DesignData.BottomPanel}">

	<UserControl.Styles>
		<Style Selector="controls|BottomPanel /template/ ToggleButton#PART_toggle">
			<Setter Property="Opacity" Value="0.65"/>
		</Style>
	</UserControl.Styles>

	<controls:BottomPanel Background="{DynamicResource MainBrush}">
		<Grid>
			<Border Opacity="0.65"
					Background="{DynamicResource MainBrush}"/>
			<panels:SnapshotMakerView Padding="8"
									  DataContext="{Binding SnapshotMaker}"/>
		</Grid>
	</controls:BottomPanel>

</UserControl>
