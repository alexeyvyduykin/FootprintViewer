namespace FootprintViewer.Data
{
    public enum ProviderType
    {
        Footprints,
        GroundTargets,
        GroundStations,
        Satellites,
        UserGeometries,
        FootprintPreviewGeometries,
        MapBackgrounds,
        FootprintPreviews
    };
}
