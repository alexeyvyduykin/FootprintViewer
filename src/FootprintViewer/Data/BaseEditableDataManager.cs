using System.Threading.Tasks;

namespace FootprintViewer.Data
{
    //public abstract class BaseEditableDataManager<TNative, TSource> : BaseDataManager<TNative, TSource>, IEditableDataManager<TNative> where TSource : IDataSource
    //{
    //    Task IEditableDataManager<TNative>.AddAsync(IDataSource dataSource, TNative value)
    //    {
    //        return AddAsync((TSource)dataSource, value);
    //    }

    //    Task IEditableDataManager<TNative>.EditAsync(IDataSource dataSource, string key, TNative value)
    //    {
    //        return EditAsync((TSource)dataSource, key, value);
    //    }

    //    Task IEditableDataManager<TNative>.RemoveAsync(IDataSource dataSource, TNative value)
    //    {
    //        return RemoveAsync((TSource)dataSource, value);
    //    }

    //    protected abstract Task AddAsync(TSource dataSource, TNative value);

    //    protected abstract Task EditAsync(TSource dataSource, string key, TNative value);

    //    protected abstract Task RemoveAsync(TSource dataSource, TNative value);
    //}
}
